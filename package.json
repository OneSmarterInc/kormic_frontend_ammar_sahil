{
  "name": "kormic-unified-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.16"
  },
  "scripts": {
    "setup": "node scripts/setup.mjs",
    "prebuild": "npm run setup",
    "test": "node --test tests/*.test.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs"
  }
}
