# Backend origin only, without /api. Build forwards the same origin to all portals.
KORMIC_API_ORIGIN=https://backend.kormic.ai
# For local testing, use http://127.0.0.1:8000 and open the frontend on 127.0.0.1 too.
